lista = ['a', 'b', 'c', 'd']
indices = range(len(lista))  # obs: começou pelo zero e foi ate 3, indicando dessa forma que há 4 indices na lista.

for indice in indices:
    print(indice, lista[indice])